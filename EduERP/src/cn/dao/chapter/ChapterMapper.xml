<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dao.chapter.ChapterMapper">
<select id="findBy" resultType="Chapter" parameterType="Chapter">
		select * from Chapter where 1 = 1
			<if test="chaid != null">
				and chaId = #{chaId},
			</if>
			<if test="chaName != null">
				and chaName = #{chaName},
			</if>
			<if test="chaNumber != null">
				and chaNumber = #{chaNubmer},
			</if>
	</select>
	<resultMap type="Chapter" id="ChpaterList">
		<id property="id" column="id"/>
		<result property="chaId" column="chaId"/>
		<result property="chaName" column="chaName"/>
		<result property="chaNumber" column="chaNumber"/>
	</resultMap>
	<!-- <resultMap type="Chapter" id="ChpaterList">
		<id property="id" column="id"/>
		<result property="chaId" column="chaId"/>
		<result property="chaName" column="chaName"/>
		<result property="chaNumber" column="chaNumber"/>
	</resultMap>
	查询章节信息
	<select id="findBy" resultType="Chapter">
		select * from Chapter where 1 = 1
			<if test="chaid != null">
				and chaId = #{chaId},
			</if>
			<if test="chaName != null">
				and chaName = #{chaName},
			</if>
			<if test="chaNumber != null">
				and chaNumber = #{chaNubmer},
			</if>
	</select>
	添加章节
	<insert id="insert"  parameterType="Integer">
		insert into Chapter(chaId,chaName,chaNumber)
			values(#{chaId},#{chaName},#{chaNumber})
	</insert>
	修改章节
	<update id="update" parameterType="Integer">
		update chapter
		<set>
			<if test="chaId != null">chaId=#{chaId}</if>
			<if test="chaName != null">chaName=#{chaName}</if>
			<if test="chaNumber !=null">chaNumber=#{chaNumber}</if>
		</set>
		where id = #{id}
	</update>
	删除章节
	<delete id="delete" parameterType="Integer">
		delete form chapter where id=#{id}
	</delete> -->
</mapper>